---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: httpstatuspollers.healthchecker.cattle.io
spec:
  group: healthchecker.cattle.io
  names:
    kind: HttpStatusPoller
    listKind: HttpStatusPollerList
    plural: httpstatuspollers
    singular: httpstatuspoller
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: HttpStatusPoller is the Schema for the httpstatuspollers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HttpStatusPollerSpec defines the desired state of HttpStatusPoller
            properties:
              intervalSeconds:
                description: IntervalSeconds specifies how many seconds to wait between
                  subsequent checks
                type: integer
              urls:
                description: URLs is a slice containing the URLs to check
                items:
                  type: string
                minItems: 1
                type: array
            required:
            - urls
            type: object
          status:
            description: HttpStatusPollerStatus defines the observed state of HttpStatusPoller
            properties:
              statusCodes:
                additionalProperties:
                  type: integer
                description: |-
                  StatusCodes tracks the HTTP return code of the checked URLs or -1 if
                  the URL can't be reached
                type: object
            required:
            - statusCodes
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
